# Read-API environment variables

# Reddit OAuth
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_REDIRECT_URI=
USER_AGENT=Reddzit/1.0

# CORS (optional; defaults to permissive if unset)
# In local dev, set to your Vite dev server origin
# e.g., http://localhost:5173
CORS_ORIGIN=

# Share preview SSR
# Path to built frontend (contains index.html and assets/)
FRONTEND_DIST_DIR=/var/www/reddzit-refresh/dist
# Public origin for absolute URLs in meta tags
PUBLIC_BASE_URL=https://reddzit.seojeek.com
